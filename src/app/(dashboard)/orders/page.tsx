import { PageCard } from "@/components/page-card";

const OrderPage = () => {
  return (
    <div>
      <PageCard title="Orders" subtitle="All orders in your store">
        <h1>Orders list</h1>
      </PageCard>
    </div>
  );
};

export default OrderPage;
